<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".debug.DebugTaskReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.agents.arya.DEBUG_TASK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".debug.TaskTriggerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.agents.arya.TASK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
