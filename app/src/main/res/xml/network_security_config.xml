<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- All network endpoints, including custom cloud providers, must use TLS. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
