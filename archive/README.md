# Archived source (excluded from the Android build)

These trees were removed from `app/src` per S8. Git history still has them.

| Path | Why archived |
|---|---|
| `hermes/` | Parallel Hermes agent loop, cron, MCP, memory/skills, voice tools |
| `tests/hermes/` | Unit tests that only covered archived Hermes types |

Kept in the live tree: `agent/hermes/safety/` (SensitiveActionGate + ConfirmActivity).
