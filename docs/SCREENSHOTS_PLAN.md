# Screenshot / GIF plan (overflow #10)

Record on a mid-range phone after assembleDebug.

1. `first-run-permissions.gif` — GuideActivity 3 steps + live PermissionTruth.
2. `tier1-open-telegram.gif` — “تلگرامو باز کن” <1s, no model notification.
3. `voice-listen.gif` — mic → pulsing sheet → transcript.
4. `overlay-sheet.gif` — floating circle over Chrome → compact chat.
5. `engine-kill.png` — logcat + UI error chip after `kill -9 :engine`.
6. `settings-model-gating.png` — 4B row disabled on a 4 GB emulator.
